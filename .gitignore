/.cache
/.work
/_output
/bin
/vendor
/.vendor-new
cover.out
*.test
*.out
.DS_Store

# IDE folders
.vscode/
.idea/

# Internal docs (planning notes, vendored OpenAPI spec) — kept locally, not pushed.
docs/

# TF provider schema is regenerated by `make generate` from the pinned TF
# provider version; keeping it local avoids enormous diffs on each bump.
config/schema.json
