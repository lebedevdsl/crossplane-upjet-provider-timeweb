---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: loadbalancers.lb.timeweb.crossplane.io
spec:
  group: lb.timeweb.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - twc
    kind: LoadBalancer
    listKind: LoadBalancerList
    plural: loadbalancers
    singular: loadbalancer
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancer is the Schema for the LoadBalancers API. Resource
          for describing needed load balancer and provides actual information about
          its status
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadBalancerSpec defines the desired state of LoadBalancer
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  algo:
                    description: |-
                      (String) Algorithm for balancing
                      Algorithm for balancing
                    type: string
                  availabilityZone:
                    description: |-
                      (String) Availability zone for load balancer
                      Availability zone for load balancer
                    type: string
                  clientTimeout:
                    description: |-
                      (Number) Client timeout
                      Client timeout
                    type: number
                  connectTimeout:
                    description: |-
                      (Number) Connection timeout
                      Connection timeout
                    type: number
                  floatingIpId:
                    description: |-
                      (String) Floating IP ID for server
                      Floating IP ID for server
                    type: string
                  floatingIpIdRef:
                    description: Reference to a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  floatingIpIdSelector:
                    description: Selector for a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  healthCheck:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        fall:
                          description: |-
                            (Number) Error requests count threshold for active backend health check
                            Error requests count threshold for active backend health check
                          type: number
                        inter:
                          description: |-
                            (Number) Interval in seconds for active backend health check
                            Interval in seconds for active backend health check
                          type: number
                        path:
                          description: |-
                            (String) Path for active backend health check
                            Path for active backend health check
                          type: string
                        port:
                          description: |-
                            (Number) TCP port for active backend health check
                            TCP port for active backend health check
                          type: number
                        proto:
                          description: |-
                            (String) Protocol for active backend health check
                            Protocol for active backend health check
                          type: string
                        rise:
                          description: |-
                            (Number) Success requests count threshold for active backend health check
                            Success requests count threshold for active backend health check
                          type: number
                        timeout:
                          description: |-
                            (Number) Timeout for active backend health check
                            Timeout for active backend health check
                          type: number
                      type: object
                    type: array
                  httprequestTimeout:
                    description: |-
                      (Number) Http request timeout
                      Http request timeout
                    type: number
                  ips:
                    description: |-
                      (Set of String) Backends IPs
                      Backends IPs
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  isKeepalive:
                    description: |-
                      (Boolean) Keep alive connection from balancer to backend server
                      Keep alive connection from balancer to backend server
                    type: boolean
                  isSsl:
                    description: |-
                      (Boolean) Automatic redirect HTTP to HTTPS
                      Automatic redirect HTTP to HTTPS
                    type: boolean
                  isSticky:
                    description: |-
                      (Boolean) Save user session for balancing to same backend server
                      Save user session for balancing to same backend server
                    type: boolean
                  isUseProxy:
                    description: |-
                      protocol for communicating with backend server
                      Use PROXY-protocol for communicating with backend server
                    type: boolean
                  localNetwork:
                    description: |-
                      (Block List, Max: 1) Flag that enables local network for load balancer (see below for nested schema)
                      Flag that enables local network for load balancer
                    items:
                      properties:
                        id:
                          description: |-
                            (String) The ID of this resource.
                            ID of VPC for assign address from
                          type: string
                        ip:
                          description: |-
                            address balancer of network interface
                            Address in VPC subnetwork for manual assign
                          type: string
                      type: object
                    type: array
                  maxconn:
                    description: |-
                      (Number) Maximum number of connections to backend server
                      Maximum number of connections to backend server
                    type: number
                  name:
                    description: |-
                      (String) Name for balancer
                      Name for balancer
                    type: string
                  presetId:
                    description: |-
                      (Number) Preset ID for balancer
                      Preset ID for balancer
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for created balancer
                      Project ID for created balancer
                    type: number
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serverTimeout:
                    description: |-
                      (Number) Server timeout
                      Server timeout
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  algo:
                    description: |-
                      (String) Algorithm for balancing
                      Algorithm for balancing
                    type: string
                  availabilityZone:
                    description: |-
                      (String) Availability zone for load balancer
                      Availability zone for load balancer
                    type: string
                  clientTimeout:
                    description: |-
                      (Number) Client timeout
                      Client timeout
                    type: number
                  connectTimeout:
                    description: |-
                      (Number) Connection timeout
                      Connection timeout
                    type: number
                  floatingIpId:
                    description: |-
                      (String) Floating IP ID for server
                      Floating IP ID for server
                    type: string
                  floatingIpIdRef:
                    description: Reference to a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  floatingIpIdSelector:
                    description: Selector for a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  healthCheck:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        fall:
                          description: |-
                            (Number) Error requests count threshold for active backend health check
                            Error requests count threshold for active backend health check
                          type: number
                        inter:
                          description: |-
                            (Number) Interval in seconds for active backend health check
                            Interval in seconds for active backend health check
                          type: number
                        path:
                          description: |-
                            (String) Path for active backend health check
                            Path for active backend health check
                          type: string
                        port:
                          description: |-
                            (Number) TCP port for active backend health check
                            TCP port for active backend health check
                          type: number
                        proto:
                          description: |-
                            (String) Protocol for active backend health check
                            Protocol for active backend health check
                          type: string
                        rise:
                          description: |-
                            (Number) Success requests count threshold for active backend health check
                            Success requests count threshold for active backend health check
                          type: number
                        timeout:
                          description: |-
                            (Number) Timeout for active backend health check
                            Timeout for active backend health check
                          type: number
                      type: object
                    type: array
                  httprequestTimeout:
                    description: |-
                      (Number) Http request timeout
                      Http request timeout
                    type: number
                  ips:
                    description: |-
                      (Set of String) Backends IPs
                      Backends IPs
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  isKeepalive:
                    description: |-
                      (Boolean) Keep alive connection from balancer to backend server
                      Keep alive connection from balancer to backend server
                    type: boolean
                  isSsl:
                    description: |-
                      (Boolean) Automatic redirect HTTP to HTTPS
                      Automatic redirect HTTP to HTTPS
                    type: boolean
                  isSticky:
                    description: |-
                      (Boolean) Save user session for balancing to same backend server
                      Save user session for balancing to same backend server
                    type: boolean
                  isUseProxy:
                    description: |-
                      protocol for communicating with backend server
                      Use PROXY-protocol for communicating with backend server
                    type: boolean
                  localNetwork:
                    description: |-
                      (Block List, Max: 1) Flag that enables local network for load balancer (see below for nested schema)
                      Flag that enables local network for load balancer
                    items:
                      properties:
                        id:
                          description: |-
                            (String) The ID of this resource.
                            ID of VPC for assign address from
                          type: string
                        ip:
                          description: |-
                            address balancer of network interface
                            Address in VPC subnetwork for manual assign
                          type: string
                      type: object
                    type: array
                  maxconn:
                    description: |-
                      (Number) Maximum number of connections to backend server
                      Maximum number of connections to backend server
                    type: number
                  name:
                    description: |-
                      (String) Name for balancer
                      Name for balancer
                    type: string
                  presetId:
                    description: |-
                      (Number) Preset ID for balancer
                      Preset ID for balancer
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for created balancer
                      Project ID for created balancer
                    type: number
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serverTimeout:
                    description: |-
                      (Number) Server timeout
                      Server timeout
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.presetId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.presetId)
                || (has(self.initProvider) && has(self.initProvider.presetId))'
          status:
            description: LoadBalancerStatus defines the observed state of LoadBalancer.
            properties:
              atProvider:
                properties:
                  algo:
                    description: |-
                      (String) Algorithm for balancing
                      Algorithm for balancing
                    type: string
                  availabilityZone:
                    description: |-
                      (String) Availability zone for load balancer
                      Availability zone for load balancer
                    type: string
                  clientTimeout:
                    description: |-
                      (Number) Client timeout
                      Client timeout
                    type: number
                  connectTimeout:
                    description: |-
                      (Number) Connection timeout
                      Connection timeout
                    type: number
                  floatingIpId:
                    description: |-
                      (String) Floating IP ID for server
                      Floating IP ID for server
                    type: string
                  healthCheck:
                    description: '(Block List, Max: 1) (see below for nested schema)'
                    items:
                      properties:
                        fall:
                          description: |-
                            (Number) Error requests count threshold for active backend health check
                            Error requests count threshold for active backend health check
                          type: number
                        inter:
                          description: |-
                            (Number) Interval in seconds for active backend health check
                            Interval in seconds for active backend health check
                          type: number
                        path:
                          description: |-
                            (String) Path for active backend health check
                            Path for active backend health check
                          type: string
                        port:
                          description: |-
                            (Number) TCP port for active backend health check
                            TCP port for active backend health check
                          type: number
                        proto:
                          description: |-
                            (String) Protocol for active backend health check
                            Protocol for active backend health check
                          type: string
                        rise:
                          description: |-
                            (Number) Success requests count threshold for active backend health check
                            Success requests count threshold for active backend health check
                          type: number
                        timeout:
                          description: |-
                            (Number) Timeout for active backend health check
                            Timeout for active backend health check
                          type: number
                      type: object
                    type: array
                  httprequestTimeout:
                    description: |-
                      (Number) Http request timeout
                      Http request timeout
                    type: number
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ip:
                    description: |-
                      address balancer of network interface
                      IP-address balancer of network interface
                    type: string
                  ips:
                    description: |-
                      (Set of String) Backends IPs
                      Backends IPs
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  isKeepalive:
                    description: |-
                      (Boolean) Keep alive connection from balancer to backend server
                      Keep alive connection from balancer to backend server
                    type: boolean
                  isSsl:
                    description: |-
                      (Boolean) Automatic redirect HTTP to HTTPS
                      Automatic redirect HTTP to HTTPS
                    type: boolean
                  isSticky:
                    description: |-
                      (Boolean) Save user session for balancing to same backend server
                      Save user session for balancing to same backend server
                    type: boolean
                  isUseProxy:
                    description: |-
                      protocol for communicating with backend server
                      Use PROXY-protocol for communicating with backend server
                    type: boolean
                  localIp:
                    description: |-
                      address balancer of network interface
                      Local IP-address balancer of network interface
                    type: string
                  localNetwork:
                    description: |-
                      (Block List, Max: 1) Flag that enables local network for load balancer (see below for nested schema)
                      Flag that enables local network for load balancer
                    items:
                      properties:
                        id:
                          description: |-
                            (String) The ID of this resource.
                            ID of VPC for assign address from
                          type: string
                        ip:
                          description: |-
                            address balancer of network interface
                            Address in VPC subnetwork for manual assign
                          type: string
                      type: object
                    type: array
                  maxconn:
                    description: |-
                      (Number) Maximum number of connections to backend server
                      Maximum number of connections to backend server
                    type: number
                  name:
                    description: |-
                      (String) Name for balancer
                      Name for balancer
                    type: string
                  presetId:
                    description: |-
                      (Number) Preset ID for balancer
                      Preset ID for balancer
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for created balancer
                      Project ID for created balancer
                    type: number
                  serverTimeout:
                    description: |-
                      (Number) Server timeout
                      Server timeout
                    type: number
                  status:
                    description: |-
                      (String) Current status of balancer (started, starting, stoped, no_paid)
                      Current status of balancer (`started`, `starting`, `stoped`, `no_paid`)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
