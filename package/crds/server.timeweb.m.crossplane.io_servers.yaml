---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: servers.server.timeweb.m.crossplane.io
spec:
  group: server.timeweb.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - twc
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the Servers API. Resource for describing
          needed server and provides actual information about its status
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerSpec defines the desired state of Server
            properties:
              forProvider:
                properties:
                  availabilityZone:
                    description: |-
                      1, spb-3, 'spb-4', msk-1, nsk-1, 'ams-1', 'fra-1', 'ala-1')
                      Availability zone for server (`spb-1`, `spb-3`, 'spb-4', `msk-1`, `nsk-1`, 'ams-1', 'fra-1', 'ala-1')
                    type: string
                  bandwidth:
                    description: |-
                      (Number) Bandwidth for server
                      Bandwidth for server
                    type: number
                  cloudInit:
                    description: |-
                      init script. If script requires external network you should use floating_ip_id field instead of twc_floating_ip.resource
                      Cloud-init script. If script requires external network you should use floating_ip_id field instead of twc_floating_ip.resource
                    type: string
                  comment:
                    description: |-
                      (String) Comment for server
                      Comment for server
                    type: string
                  configuration:
                    description: |-
                      (Block List, Max: 1) Configuration for server (see below for nested schema)
                      Configuration for server
                    items:
                      properties:
                        configuratorId:
                          description: |-
                            (Number) Configurator identifier
                            Configurator ID for created server
                          type: number
                        cpu:
                          description: |-
                            (Number) Current CPUs count of server
                            CPU count for created server
                          type: number
                        disk:
                          description: |-
                            (Number) Disk size for created server
                            Disk size for created server
                          type: number
                        gpu:
                          description: |-
                            (Number) GPU count for created server
                            GPU count for created server
                          type: number
                        ram:
                          description: |-
                            (Number) Current RAM size of server
                            RAM size for created server (must be divisible by 1024 and corresponds OS, software and configurator requirements)
                          type: number
                      type: object
                    type: array
                  floatingIpId:
                    description: |-
                      (String) Floating IP ID for server
                      Floating IP ID for server
                    type: string
                  floatingIpIdRef:
                    description: Reference to a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  floatingIpIdSelector:
                    description: Selector for a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  hostname:
                    description: |-
                      (String) Hostname of the server
                      Hostname of the server
                    type: string
                  imageId:
                    description: |-
                      (String) Image ID for servers (not supported for cloned servers)
                      Image ID for servers (not supported for cloned servers)
                    type: string
                  isRootPasswordRequired:
                    description: |-
                      (Boolean) Is root password required for server
                      Is root password required for server
                    type: boolean
                  localNetwork:
                    description: |-
                      (Block List, Max: 1) Flag that enables local network for created server (not supported for cloned servers) (see below for nested schema)
                      Flag that enables local network for created server (not supported for cloned servers)
                    items:
                      properties:
                        id:
                          description: |-
                            (String) The ID of this resource.
                            ID of VPC for assign address from
                          type: string
                        ip:
                          description: |-
                            (String) Address in VPC subnetwork for manual assign
                            Address in VPC subnetwork for manual assign
                          type: string
                        mode:
                          description: |-
                            egress and ingress traffic allowed, snat - only egress traffic allowed, no_nat - only local network traffic allowed)
                            NAT mode for created servers (`dnat_and_snat` - egress and ingress traffic allowed, `snat` - only egress traffic allowed, `no_nat` - only local network traffic allowed)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name for server
                      Name for server
                    type: string
                  osId:
                    description: |-
                      (Number) OS ID for server (not supported for cloned servers)
                      OS ID for server (not supported for cloned servers)
                    type: number
                  presetId:
                    description: |-
                      (Number) Preset ID for server (conflicts with configuration)
                      Preset ID for server (conflicts with configuration)
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for created server
                      Project ID for created server
                    type: number
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  softwareId:
                    description: |-
                      (Number) Software ID for server (not supported for cloned servers)
                      Software ID for server (not supported for cloned servers)
                    type: number
                  sourceServerId:
                    description: |-
                      (Number) Server ID for which clone should be created
                      Server ID for which clone should be created
                    type: number
                  sourceServerIdRef:
                    description: Reference to a Server in server to populate sourceServerId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  sourceServerIdSelector:
                    description: Selector for a Server in server to populate sourceServerId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  sshKeysIds:
                    description: |-
                      (Set of Number) List of SSH keys IDs for server
                      List of SSH keys IDs for server
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  sshKeysIdsRefs:
                    description: References to SSHKey in ssh to populate sshKeysIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  sshKeysIdsSelector:
                    description: Selector for a list of SSHKey in ssh to populate
                      sshKeysIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  availabilityZone:
                    description: |-
                      1, spb-3, 'spb-4', msk-1, nsk-1, 'ams-1', 'fra-1', 'ala-1')
                      Availability zone for server (`spb-1`, `spb-3`, 'spb-4', `msk-1`, `nsk-1`, 'ams-1', 'fra-1', 'ala-1')
                    type: string
                  bandwidth:
                    description: |-
                      (Number) Bandwidth for server
                      Bandwidth for server
                    type: number
                  cloudInit:
                    description: |-
                      init script. If script requires external network you should use floating_ip_id field instead of twc_floating_ip.resource
                      Cloud-init script. If script requires external network you should use floating_ip_id field instead of twc_floating_ip.resource
                    type: string
                  comment:
                    description: |-
                      (String) Comment for server
                      Comment for server
                    type: string
                  configuration:
                    description: |-
                      (Block List, Max: 1) Configuration for server (see below for nested schema)
                      Configuration for server
                    items:
                      properties:
                        configuratorId:
                          description: |-
                            (Number) Configurator identifier
                            Configurator ID for created server
                          type: number
                        cpu:
                          description: |-
                            (Number) Current CPUs count of server
                            CPU count for created server
                          type: number
                        disk:
                          description: |-
                            (Number) Disk size for created server
                            Disk size for created server
                          type: number
                        gpu:
                          description: |-
                            (Number) GPU count for created server
                            GPU count for created server
                          type: number
                        ram:
                          description: |-
                            (Number) Current RAM size of server
                            RAM size for created server (must be divisible by 1024 and corresponds OS, software and configurator requirements)
                          type: number
                      type: object
                    type: array
                  floatingIpId:
                    description: |-
                      (String) Floating IP ID for server
                      Floating IP ID for server
                    type: string
                  floatingIpIdRef:
                    description: Reference to a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  floatingIpIdSelector:
                    description: Selector for a FloatingIP in vpc to populate floatingIpId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  hostname:
                    description: |-
                      (String) Hostname of the server
                      Hostname of the server
                    type: string
                  imageId:
                    description: |-
                      (String) Image ID for servers (not supported for cloned servers)
                      Image ID for servers (not supported for cloned servers)
                    type: string
                  isRootPasswordRequired:
                    description: |-
                      (Boolean) Is root password required for server
                      Is root password required for server
                    type: boolean
                  localNetwork:
                    description: |-
                      (Block List, Max: 1) Flag that enables local network for created server (not supported for cloned servers) (see below for nested schema)
                      Flag that enables local network for created server (not supported for cloned servers)
                    items:
                      properties:
                        id:
                          description: |-
                            (String) The ID of this resource.
                            ID of VPC for assign address from
                          type: string
                        ip:
                          description: |-
                            (String) Address in VPC subnetwork for manual assign
                            Address in VPC subnetwork for manual assign
                          type: string
                        mode:
                          description: |-
                            egress and ingress traffic allowed, snat - only egress traffic allowed, no_nat - only local network traffic allowed)
                            NAT mode for created servers (`dnat_and_snat` - egress and ingress traffic allowed, `snat` - only egress traffic allowed, `no_nat` - only local network traffic allowed)
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name for server
                      Name for server
                    type: string
                  osId:
                    description: |-
                      (Number) OS ID for server (not supported for cloned servers)
                      OS ID for server (not supported for cloned servers)
                    type: number
                  presetId:
                    description: |-
                      (Number) Preset ID for server (conflicts with configuration)
                      Preset ID for server (conflicts with configuration)
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for created server
                      Project ID for created server
                    type: number
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  softwareId:
                    description: |-
                      (Number) Software ID for server (not supported for cloned servers)
                      Software ID for server (not supported for cloned servers)
                    type: number
                  sourceServerId:
                    description: |-
                      (Number) Server ID for which clone should be created
                      Server ID for which clone should be created
                    type: number
                  sourceServerIdRef:
                    description: Reference to a Server in server to populate sourceServerId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  sourceServerIdSelector:
                    description: Selector for a Server in server to populate sourceServerId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  sshKeysIds:
                    description: |-
                      (Set of Number) List of SSH keys IDs for server
                      List of SSH keys IDs for server
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  sshKeysIdsRefs:
                    description: References to SSHKey in ssh to populate sshKeysIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  sshKeysIdsSelector:
                    description: Selector for a list of SSHKey in ssh to populate
                      sshKeysIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ServerStatus defines the observed state of Server.
            properties:
              atProvider:
                properties:
                  availabilityZone:
                    description: |-
                      1, spb-3, 'spb-4', msk-1, nsk-1, 'ams-1', 'fra-1', 'ala-1')
                      Availability zone for server (`spb-1`, `spb-3`, 'spb-4', `msk-1`, `nsk-1`, 'ams-1', 'fra-1', 'ala-1')
                    type: string
                  avatarId:
                    description: |-
                      (String) Identifier of avatar
                      Identifier of avatar
                    type: string
                  bandwidth:
                    description: |-
                      (Number) Bandwidth for server
                      Bandwidth for server
                    type: number
                  bootMode:
                    description: |-
                      (String) Boot mode of server (std, single, cd)
                      Boot mode of server (`std`, `single`, `cd`)
                    type: string
                  cloudInit:
                    description: |-
                      init script. If script requires external network you should use floating_ip_id field instead of twc_floating_ip.resource
                      Cloud-init script. If script requires external network you should use floating_ip_id field instead of twc_floating_ip.resource
                    type: string
                  comment:
                    description: |-
                      (String) Comment for server
                      Comment for server
                    type: string
                  configuration:
                    description: |-
                      (Block List, Max: 1) Configuration for server (see below for nested schema)
                      Configuration for server
                    items:
                      properties:
                        configuratorId:
                          description: |-
                            (Number) Configurator identifier
                            Configurator ID for created server
                          type: number
                        cpu:
                          description: |-
                            (Number) Current CPUs count of server
                            CPU count for created server
                          type: number
                        disk:
                          description: |-
                            (Number) Disk size for created server
                            Disk size for created server
                          type: number
                        gpu:
                          description: |-
                            (Number) GPU count for created server
                            GPU count for created server
                          type: number
                        ram:
                          description: |-
                            (Number) Current RAM size of server
                            RAM size for created server (must be divisible by 1024 and corresponds OS, software and configurator requirements)
                          type: number
                      type: object
                    type: array
                  configuratorId:
                    description: |-
                      (Number) Configurator identifier
                      Configurator identifier
                    type: number
                  cpu:
                    description: |-
                      (Number) Current CPUs count of server
                      Current CPUs count of server
                    type: number
                  cpuFrequency:
                    description: |-
                      (String) Current CPU frequency of server
                      Current CPU frequency of server
                    type: string
                  disks:
                    description: |-
                      (List of Object) Current server disks representation (see below for nested schema)
                      Current server disks representation
                    items:
                      properties:
                        id:
                          description: (String) The ID of this resource.
                          type: number
                        isMounted:
                          description: (Boolean)
                          type: boolean
                        isSystem:
                          description: (Boolean)
                          type: boolean
                        size:
                          description: (Number)
                          type: number
                        status:
                          description: (String) Current status of server (installing,
                            software_install, reinstalling, on, off, turning_on, turning_off,
                            hard_turning_off, rebooting, hard_rebooting, removing,
                            removed, cloning, transfer, blocked, configuring, no_paid,
                            permanent_blocked)
                          type: string
                        systemName:
                          description: (String)
                          type: string
                        type:
                          description: (String)
                          type: string
                        used:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  floatingIpId:
                    description: |-
                      (String) Floating IP ID for server
                      Floating IP ID for server
                    type: string
                  hostname:
                    description: |-
                      (String) Hostname of the server
                      Hostname of the server
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  imageId:
                    description: |-
                      (String) Image ID for servers (not supported for cloned servers)
                      Image ID for servers (not supported for cloned servers)
                    type: string
                  isDdosGuard:
                    description: |-
                      (Boolean) Flag that shows status of DDoS guard service
                      Flag that shows status of DDoS guard service
                    type: boolean
                  isRootPasswordRequired:
                    description: |-
                      (Boolean) Is root password required for server
                      Is root password required for server
                    type: boolean
                  localNetwork:
                    description: |-
                      (Block List, Max: 1) Flag that enables local network for created server (not supported for cloned servers) (see below for nested schema)
                      Flag that enables local network for created server (not supported for cloned servers)
                    items:
                      properties:
                        id:
                          description: |-
                            (String) The ID of this resource.
                            ID of VPC for assign address from
                          type: string
                        ip:
                          description: |-
                            (String) Address in VPC subnetwork for manual assign
                            Address in VPC subnetwork for manual assign
                          type: string
                        mode:
                          description: |-
                            egress and ingress traffic allowed, snat - only egress traffic allowed, no_nat - only local network traffic allowed)
                            NAT mode for created servers (`dnat_and_snat` - egress and ingress traffic allowed, `snat` - only egress traffic allowed, `no_nat` - only local network traffic allowed)
                          type: string
                      type: object
                    type: array
                  location:
                    description: |-
                      1, ru-2, 'ru-3', de-1, kz-1, 'nl-1')
                      Location for the server (`ru-1`, `ru-2`, 'ru-3', `de-1`, `kz-1`, 'nl-1')
                    type: string
                  mainIpv4:
                    description: |-
                      (String) Main IPv4 address from public interface
                      Main IPv4 address from public interface
                    type: string
                  name:
                    description: |-
                      (String) Name for server
                      Name for server
                    type: string
                  networks:
                    description: (List of Object) (see below for nested schema)
                    items:
                      properties:
                        bandwidth:
                          description: (Number) Bandwidth for server
                          type: number
                        ips:
                          description: (List of Object) (see below for nested schema)
                          items:
                            properties:
                              ip:
                                description: (String) Address in VPC subnetwork for
                                  manual assign
                                type: string
                              isMain:
                                description: (Boolean)
                                type: boolean
                              ptr:
                                description: (String)
                                type: string
                              type:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        isDdosGuard:
                          description: (Boolean) Flag that shows status of DDoS guard
                            service
                          type: boolean
                        natMode:
                          description: (String)
                          type: string
                        type:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  os:
                    description: |-
                      (List of Object) Information about specified OS (see below for nested schema)
                      Information about specified OS
                    items:
                      properties:
                        id:
                          description: (String) The ID of this resource.
                          type: number
                        name:
                          description: (String) Name for server
                          type: string
                        version:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  osId:
                    description: |-
                      (Number) OS ID for server (not supported for cloned servers)
                      OS ID for server (not supported for cloned servers)
                    type: number
                  presetId:
                    description: |-
                      (Number) Preset ID for server (conflicts with configuration)
                      Preset ID for server (conflicts with configuration)
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for created server
                      Project ID for created server
                    type: number
                  ram:
                    description: |-
                      (Number) Current RAM size of server
                      Current RAM size of server
                    type: number
                  software:
                    description: |-
                      (List of Object) Information about specified software (if software_id was set) (see below for nested schema)
                      Information about specified software (if software_id was set)
                    items:
                      properties:
                        id:
                          description: (String) The ID of this resource.
                          type: number
                        name:
                          description: (String) Name for server
                          type: string
                      type: object
                    type: array
                  softwareId:
                    description: |-
                      (Number) Software ID for server (not supported for cloned servers)
                      Software ID for server (not supported for cloned servers)
                    type: number
                  sourceServerId:
                    description: |-
                      (Number) Server ID for which clone should be created
                      Server ID for which clone should be created
                    type: number
                  sshKeysIds:
                    description: |-
                      (Set of Number) List of SSH keys IDs for server
                      List of SSH keys IDs for server
                    items:
                      type: number
                    type: array
                    x-kubernetes-list-type: set
                  startAt:
                    description: |-
                      (String) Last boot time (in ISO8601 format)
                      Last boot time (in ISO8601 format)
                    type: string
                  status:
                    description: |-
                      (String) Current status of server (installing, software_install, reinstalling, on, off, turning_on, turning_off, hard_turning_off, rebooting, hard_rebooting, removing, removed, cloning, transfer, blocked, configuring, no_paid, permanent_blocked)
                      Current status of server (`installing`, `software_install`, `reinstalling`, `on`, `off`, `turning_on`, `turning_off`, `hard_turning_off`, `rebooting`, `hard_rebooting`, `removing`, `removed`, `cloning`, `transfer`, `blocked`, `configuring`, `no_paid`, `permanent_blocked`)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
