---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusters.k8s.timeweb.m.crossplane.io
spec:
  group: k8s.timeweb.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - twc
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the Clusters API. Resource for describing
          needed K8S cluster and provides actual information about its status
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines the desired state of Cluster
            properties:
              forProvider:
                properties:
                  configuration:
                    description: |-
                      (Block List, Max: 1) Configuration for master nodes (see below for nested schema)
                      Configuration for master nodes
                    items:
                      properties:
                        configuratorId:
                          description: |-
                            (Number) Configurator ID for creating nodes
                            Configurator ID for creating nodes
                          type: number
                        cpu:
                          description: |-
                            (Number) Sum of CPU count of all worker nodes
                            CPU count for node group
                          type: number
                        disk:
                          description: |-
                            (Number) Sum of disks volume of worker nodes
                            Disk node group size
                          type: number
                        ram:
                          description: |-
                            (Number) Sum of RAM count of all worker nodes
                            RAM node group worker size
                          type: number
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description for K8S cluster
                      Description for K8S cluster
                    type: string
                  highAvailability:
                    description: |-
                      (Boolean) High Availability for K8S cluster
                      High Availability for K8S cluster
                    type: boolean
                  ingress:
                    description: |-
                      (Boolean) Add ingress for K8S cluster
                      Add ingress for K8S cluster
                    type: boolean
                  maintenanceSlot:
                    description: |-
                      (Block List) Maintenance window for cluster (see below for nested schema)
                      Maintenance window for cluster
                    items:
                      properties:
                        from:
                          description: |-
                            (String) May start from this time (HH:MM format)
                            May start from this time (HH:MM format)
                          type: string
                        to:
                          description: |-
                            (String) May end to this time (HH:MM). Notice: The actual duration depends on the number of nodes in the cluster.
                            May end to this time (HH:MM). Notice: The actual duration depends on the number of nodes in the cluster.
                          type: string
                        type:
                          description: |-
                            (String) Maintenance slot type
                            Maintenance slot type
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name for K8S cluster
                      Name for K8S cluster
                    type: string
                  networkDriver:
                    description: |-
                      (String) Network driver for K8S cluster may be retrieved from API
                      Network driver for K8S cluster [may be retrieved from API](https://api.timeweb.cloud/api/v1/k8s/network_drivers)
                    type: string
                  networkId:
                    description: |-
                      (String) ID of VPC for K8S cluster
                      ID of VPC for K8S cluster
                    type: string
                  networkIdRef:
                    description: Reference to a VPC in vpc to populate networkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkIdSelector:
                    description: Selector for a VPC in vpc to populate networkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  oidcProvider:
                    description: |-
                      (Block List) OIDC provider settings for cluster (see below for nested schema)
                      OIDC provider settings for cluster
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) Unique identifier by authentication server
                            Unique identifier by authentication server
                          type: string
                        groupsClaim:
                          description: (String)
                          type: string
                        issuerUrl:
                          description: |-
                            (String) OIDC Provider URL
                            OIDC Provider URL
                          type: string
                        name:
                          description: |-
                            (String) Name for K8S cluster
                            Name for OIDC provider setting
                          type: string
                        usernameClaim:
                          description: |-
                            (String) Name of the field in the ID token that the OIDC authentication system uses as the username
                            Name of the field in the ID token that the OIDC authentication system uses as the username
                          type: string
                      type: object
                    type: array
                  presetId:
                    description: |-
                      (Number) Preset ID for nodes in group
                      Preset ID for nodes in group
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for K8S cluster
                      Project ID for K8S cluster
                    type: number
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  version:
                    description: |-
                      (String) K8S version may be retrieved from API. If your version is outdated, we will take the latest patch of provided version! You need to check your version after cluster installation!
                      K8S version [may be retrieved from API](https://api.timeweb.cloud/api/v1/k8s/k8s_versions). If your version is outdated, we will take the latest patch of provided version! You need to check your version after cluster installation!
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  configuration:
                    description: |-
                      (Block List, Max: 1) Configuration for master nodes (see below for nested schema)
                      Configuration for master nodes
                    items:
                      properties:
                        configuratorId:
                          description: |-
                            (Number) Configurator ID for creating nodes
                            Configurator ID for creating nodes
                          type: number
                        cpu:
                          description: |-
                            (Number) Sum of CPU count of all worker nodes
                            CPU count for node group
                          type: number
                        disk:
                          description: |-
                            (Number) Sum of disks volume of worker nodes
                            Disk node group size
                          type: number
                        ram:
                          description: |-
                            (Number) Sum of RAM count of all worker nodes
                            RAM node group worker size
                          type: number
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description for K8S cluster
                      Description for K8S cluster
                    type: string
                  highAvailability:
                    description: |-
                      (Boolean) High Availability for K8S cluster
                      High Availability for K8S cluster
                    type: boolean
                  ingress:
                    description: |-
                      (Boolean) Add ingress for K8S cluster
                      Add ingress for K8S cluster
                    type: boolean
                  maintenanceSlot:
                    description: |-
                      (Block List) Maintenance window for cluster (see below for nested schema)
                      Maintenance window for cluster
                    items:
                      properties:
                        from:
                          description: |-
                            (String) May start from this time (HH:MM format)
                            May start from this time (HH:MM format)
                          type: string
                        to:
                          description: |-
                            (String) May end to this time (HH:MM). Notice: The actual duration depends on the number of nodes in the cluster.
                            May end to this time (HH:MM). Notice: The actual duration depends on the number of nodes in the cluster.
                          type: string
                        type:
                          description: |-
                            (String) Maintenance slot type
                            Maintenance slot type
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name for K8S cluster
                      Name for K8S cluster
                    type: string
                  networkDriver:
                    description: |-
                      (String) Network driver for K8S cluster may be retrieved from API
                      Network driver for K8S cluster [may be retrieved from API](https://api.timeweb.cloud/api/v1/k8s/network_drivers)
                    type: string
                  networkId:
                    description: |-
                      (String) ID of VPC for K8S cluster
                      ID of VPC for K8S cluster
                    type: string
                  networkIdRef:
                    description: Reference to a VPC in vpc to populate networkId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  networkIdSelector:
                    description: Selector for a VPC in vpc to populate networkId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  oidcProvider:
                    description: |-
                      (Block List) OIDC provider settings for cluster (see below for nested schema)
                      OIDC provider settings for cluster
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) Unique identifier by authentication server
                            Unique identifier by authentication server
                          type: string
                        groupsClaim:
                          description: (String)
                          type: string
                        issuerUrl:
                          description: |-
                            (String) OIDC Provider URL
                            OIDC Provider URL
                          type: string
                        name:
                          description: |-
                            (String) Name for K8S cluster
                            Name for OIDC provider setting
                          type: string
                        usernameClaim:
                          description: |-
                            (String) Name of the field in the ID token that the OIDC authentication system uses as the username
                            Name of the field in the ID token that the OIDC authentication system uses as the username
                          type: string
                      type: object
                    type: array
                  presetId:
                    description: |-
                      (Number) Preset ID for nodes in group
                      Preset ID for nodes in group
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for K8S cluster
                      Project ID for K8S cluster
                    type: number
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  version:
                    description: |-
                      (String) K8S version may be retrieved from API. If your version is outdated, we will take the latest patch of provided version! You need to check your version after cluster installation!
                      K8S version [may be retrieved from API](https://api.timeweb.cloud/api/v1/k8s/k8s_versions). If your version is outdated, we will take the latest patch of provided version! You need to check your version after cluster installation!
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.networkDriver is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.networkDriver)
                || (has(self.initProvider) && has(self.initProvider.networkDriver))'
            - message: spec.forProvider.version is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.version)
                || (has(self.initProvider) && has(self.initProvider.version))'
          status:
            description: ClusterStatus defines the observed state of Cluster.
            properties:
              atProvider:
                properties:
                  configuration:
                    description: |-
                      (Block List, Max: 1) Configuration for master nodes (see below for nested schema)
                      Configuration for master nodes
                    items:
                      properties:
                        configuratorId:
                          description: |-
                            (Number) Configurator ID for creating nodes
                            Configurator ID for creating nodes
                          type: number
                        cpu:
                          description: |-
                            (Number) Sum of CPU count of all worker nodes
                            CPU count for node group
                          type: number
                        disk:
                          description: |-
                            (Number) Sum of disks volume of worker nodes
                            Disk node group size
                          type: number
                        ram:
                          description: |-
                            (Number) Sum of RAM count of all worker nodes
                            RAM node group worker size
                          type: number
                      type: object
                    type: array
                  cpu:
                    description: |-
                      (Number) Sum of CPU count of all worker nodes
                      Sum of CPU count of all worker nodes
                    type: number
                  description:
                    description: |-
                      (String) Description for K8S cluster
                      Description for K8S cluster
                    type: string
                  disk:
                    description: |-
                      (Number) Sum of disks volume of worker nodes
                      Sum of disks volume of worker nodes
                    type: number
                  highAvailability:
                    description: |-
                      (Boolean) High Availability for K8S cluster
                      High Availability for K8S cluster
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  ingress:
                    description: |-
                      (Boolean) Add ingress for K8S cluster
                      Add ingress for K8S cluster
                    type: boolean
                  maintenanceSlot:
                    description: |-
                      (Block List) Maintenance window for cluster (see below for nested schema)
                      Maintenance window for cluster
                    items:
                      properties:
                        from:
                          description: |-
                            (String) May start from this time (HH:MM format)
                            May start from this time (HH:MM format)
                          type: string
                        to:
                          description: |-
                            (String) May end to this time (HH:MM). Notice: The actual duration depends on the number of nodes in the cluster.
                            May end to this time (HH:MM). Notice: The actual duration depends on the number of nodes in the cluster.
                          type: string
                        type:
                          description: |-
                            (String) Maintenance slot type
                            Maintenance slot type
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name for K8S cluster
                      Name for K8S cluster
                    type: string
                  networkDriver:
                    description: |-
                      (String) Network driver for K8S cluster may be retrieved from API
                      Network driver for K8S cluster [may be retrieved from API](https://api.timeweb.cloud/api/v1/k8s/network_drivers)
                    type: string
                  networkId:
                    description: |-
                      (String) ID of VPC for K8S cluster
                      ID of VPC for K8S cluster
                    type: string
                  oidcProvider:
                    description: |-
                      (Block List) OIDC provider settings for cluster (see below for nested schema)
                      OIDC provider settings for cluster
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) Unique identifier by authentication server
                            Unique identifier by authentication server
                          type: string
                        groupsClaim:
                          description: (String)
                          type: string
                        issuerUrl:
                          description: |-
                            (String) OIDC Provider URL
                            OIDC Provider URL
                          type: string
                        name:
                          description: |-
                            (String) Name for K8S cluster
                            Name for OIDC provider setting
                          type: string
                        status:
                          description: (String) Current status of K8S cluster (installing,
                            provisioning, active, unpaid, started, not_installed,
                            failed, unpaid_not_installed, deletion)
                          type: string
                        usernameClaim:
                          description: |-
                            (String) Name of the field in the ID token that the OIDC authentication system uses as the username
                            Name of the field in the ID token that the OIDC authentication system uses as the username
                          type: string
                      type: object
                    type: array
                  presetId:
                    description: |-
                      (Number) Preset ID for nodes in group
                      Preset ID for nodes in group
                    type: number
                  projectId:
                    description: |-
                      (Number) Project ID for K8S cluster
                      Project ID for K8S cluster
                    type: number
                  ram:
                    description: |-
                      (Number) Sum of RAM count of all worker nodes
                      Sum of RAM count of all worker nodes
                    type: number
                  status:
                    description: |-
                      (String) Current status of K8S cluster (installing, provisioning, active, unpaid, started, not_installed, failed, unpaid_not_installed, deletion)
                      Current status of K8S cluster (`installing`, `provisioning`, `active`, `unpaid`, `started`, `not_installed`, `failed`, `unpaid_not_installed`, `deletion`)
                    type: string
                  version:
                    description: |-
                      (String) K8S version may be retrieved from API. If your version is outdated, we will take the latest patch of provided version! You need to check your version after cluster installation!
                      K8S version [may be retrieved from API](https://api.timeweb.cloud/api/v1/k8s/k8s_versions). If your version is outdated, we will take the latest patch of provided version! You need to check your version after cluster installation!
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
